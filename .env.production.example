# Copiez ces variables dans Vercel > Settings > Environment Variables
# (Production), puis redéployez : Vite les intègre au moment du build.

# URL publique HTTPS de votre backend une fois déployé
# (Railway, Render, Fly.io...). Sans slash final.
VITE_API_URL=https://votre-backend.onrender.com
